/**
 * Registers every module's contributions to other modules' screens (see lib/slots.ts).
 * Modules add one import line each.
 */
import './project-tabs';
import './knowledge/register-slots';
import './tasks/register-slots';
import './ofm/register-slots';
