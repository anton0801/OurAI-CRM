import { registerLabels } from '@/lib/labels';

/** Labels for the Overview (S08). */
registerLabels({
  attentionKind: {
    overdue_task: 'Overdue task',
    blocked_task: 'Blocked task',
    review_waiting: 'Review waiting',
    publication_unapproved: 'Unapproved publication',
    checkpoint_missing: 'Missing checkpoint',
    shift_end_forgotten: 'Shift not ended',
    budget_overspent: 'Budget over plan',
  },
});
