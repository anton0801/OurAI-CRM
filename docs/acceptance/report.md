# Acceptance report

Generated 2026-09-24T17:56:19.352Z from commit `c73bbab` by `pnpm acceptance:report`.
Source of scenarios: spec §29 (`docs/spec/08-operations-acceptance.md`). Automated evidence is
matched by the T-id in the test name; manual/operational evidence is kept in
`docs/acceptance/manual-evidence.json`.

| | Count |
|---|---|
| Scenarios | 172 |
| Covered by passing automated tests | 171 |
| Failing (automated tests, or measured and not met) | 1 |
| Manual / operational evidence only | 0 |
| Without evidence | 0 |
| Vitest results (all tests) | 719 passed, 0 failed, 0 skipped |
| Playwright results | 22 passed, 0 failed, 0 skipped |

| ID | Scenario | Expected | Status | Evidence |
|---|---|---|---|---|
| T001 | Новый production workspace | Нет демопроектов, fake KPIs и default admin password | ✅ automated | integration: `tests/integration/identity/auth.test.ts` — bootstrap a new production workspace has no demo data, no fake KPIs and no default password (T001) |
| T002 | Повтор bootstrap | Второй Owner не создаётся, команда объясняет уже завершённый bootstrap | ✅ automated | integration: `tests/integration/identity/auth.test.ts` — bootstrap creates exactly one Owner with a one-time password and refuses a second bootstrap (T002) |
| T003 | Первый вход Owner | MFA и три шага настройки доступны, progress переживает reload | ✅ automated | e2e: `tests/e2e/specs/first-run.setup.ts` — first run of the Owner (F01, T003) |
| T004 | Invite accepted дважды | Один user/membership, второй запрос безопасно возвращает текущее состояние | ✅ automated | integration: `tests/integration/identity/auth.test.ts` — invitations accepting twice creates one user and one membership (T004)<br>integration: `tests/integration/team/invitations.test.ts` — invitations from Team (F02) invites with role+scope, tracks delivery separately, accepting twice keeps one user and membership (T004) |
| T005 | Invite expired/revoked | Доступ не выдан, понятный recovery action | ✅ automated | integration: `tests/integration/identity/auth.test.ts` — invitations resend revokes the previous token; expired invitations grant nothing (T005, T006)<br>integration: `tests/integration/team/invitations.test.ts` — invitations from Team (F02) expired or revoked invitations grant nothing and offer Request New Invitation; admins resolve it (T005) |
| T006 | Resend invitation | Старый token недействителен, email job имеет реальный статус | ✅ automated | integration: `tests/integration/identity/auth.test.ts` — invitations resend revokes the previous token; expired invitations grant nothing (T005, T006)<br>integration: `tests/integration/team/invitations.test.ts` — invitations from Team (F02) resend revokes the previous token and queues a new mail (T006) |
| T007 | Password recovery unknown email | Ответ не раскрывает наличие аккаунта | ✅ automated | security: `tests/security/api-security.test.ts` — API security does not reveal whether an e-mail exists on password recovery (T007)<br>integration: `tests/integration/identity/auth.test.ts` — sign-in recovery response is identical for unknown and known e-mails (T007) |
| T008 | Reset token replay | Второе использование rejected, старые сессии revoked | ✅ automated | integration: `tests/integration/identity/auth.test.ts` — password reset and MFA failures (T008, T009) a reset token works once; the second use is rejected and the old sessions are revoked (T008)<br>integration: `tests/integration/identity/auth.test.ts` — password reset and MFA failures (T008, T009) wrong or repeated TOTP codes create no session, are rate-limited and audited without the code (T009) |
| T009 | TOTP неверный/повторный | Нет сессии, rate limit, audit без кода | ✅ automated | integration: `tests/integration/identity/auth.test.ts` — sign-in owner must set up MFA; TOTP codes cannot be replayed; a recovery code works once (T009, T010)<br>integration: `tests/integration/identity/auth.test.ts` — password reset and MFA failures (T008, T009) a reset token works once; the second use is rejected and the old sessions are revoked (T008)<br>integration: `tests/integration/identity/auth.test.ts` — password reset and MFA failures (T008, T009) wrong or repeated TOTP codes create no session, are rate-limited and audited without the code (T009) |
| T010 | Recovery code повторно | Второе использование запрещено | ✅ automated | integration: `tests/integration/identity/auth.test.ts` — sign-in owner must set up MFA; TOTP codes cannot be replayed; a recovery code works once (T009, T010) |
| T011 | Session expiry во время edit | Нет fake save, после входа возврат к доступному маршруту | ✅ automated | integration: `tests/integration/identity/auth.test.ts` — CSRF and session an edit with an expired session is refused without a fake save; sign-in returns to a permitted route (T011) |
| T012 | Последний Owner деактивируется | Операция блокируется | ✅ automated | integration: `tests/integration/team/lifecycle.test.ts` — deactivation (F12) the workspace Owner cannot be deactivated or suspended (T012) |
| T013 | Ownership transfer | Только после принятия и recent auth обоих, atomically no ownerless workspace | ✅ automated | integration: `tests/integration/team/ownership.test.ts` — ownership transfer (T013) needs the Owner’s recent authentication to propose and the recipient’s MFA-backed recent authentication to accept; swaps atomically<br>integration: `tests/integration/team/ownership.test.ts` — ownership transfer (T013) the recipient can decline and the Owner can cancel; expired transfers cannot be accepted |
| T014 | Contractor запрашивает project detail | Только разрешённая task projection, полного project payload нет | ✅ automated | integration: `tests/integration/work/tasks.test.ts` — task access scope a contractor requesting project detail gets no project payload, only the permitted task projection (T014) |
| T015 | Cross-workspace object ID | 404, zero mutation, tenant foreign key не позволяет link | ✅ automated | security: `tests/security/api-security.test.ts` — API security never reveals or links objects of another workspace (T015)<br>integration: `tests/integration/finance/ledger.test.ts` — finance access (scope isolation) a foreign workspace id is 404 and cross-workspace references are refused (T015)<br>integration: `tests/integration/organization/projects.test.ts` — project access scope a workspace id the member does not belong to is 404 (T015)<br>integration: `tests/integration/team/members.test.ts` — team roster (S61) cross-workspace member ids are 404 (T015) |
| T016 | Скрытый finance field в API | Отсутствует для обычного Lead и в export/search/overview | ✅ automated | integration: `tests/integration/finance/ledger.test.ts` — finance access (scope isolation) a normal Lead gets no finance data: 403 on finance, no budget in API, export, search or Overview (T016)<br>integration: `tests/integration/finance/reconciliation.test.ts` — deals and campaign panels (T138, T016) a won deal creates no income; members without finance rights see no amounts<br>integration: `tests/integration/organization/projects.test.ts` — project access scope scoped roles see only their projects; out-of-scope ids are 404; budget field absent without finance rights (T016)<br>integration: `tests/integration/overview/overview.test.ts` — overview (S08) applies scope before aggregation and omits finance without access (T016) |
| T017 | Role revoked при открытой вкладке | Новые reads/writes запрещены, stream отключён, caches очищаются | ✅ automated | integration: `tests/integration/identity/access-cache.test.ts` — access snapshot cache serves warm requests from the cache; revoking a role applies to the very next request (T017)<br>integration: `tests/integration/organization/projects.test.ts` — project access scope revoking a role takes effect on the next read and write, with zero mutation (T017)<br>integration: `tests/integration/team/access.test.ts` — role grants and effective access role revoked while a tab is open: next reads/writes are refused and the stream is told (T017) |
| T018 | Admin выдаёт себе Owner/finance manage | Нельзя без Owner grant/transfer | ✅ automated | integration: `tests/integration/team/access.test.ts` — role grants and effective access an Admin cannot give themselves Owner or finance rights by any path (T018) |
| T019 | Deactivate member с задачами/сменами | Impact preview, выбранная передача, sessions revoked, historical author сохранён | ✅ automated | integration: `tests/integration/team/lifecycle.test.ts` — deactivation (F12) impact preview → chosen successors → transfer, revoke roles and sessions, keep history (T019)<br>integration: `tests/integration/team/lifecycle.test.ts` — deactivation (F12) a member with scheduled OFM shifts: the preview lists them; the chosen successor gets one, the other is cancelled with history (T019)<br>integration: `tests/integration/team/lifecycle.test.ts` — deactivation (F12) a member on a running OFM shift cannot be deactivated until the shift is ended; the preview says so (T019) |
| T020 | Restore member | Не восстанавливает чувствительные grants молча | ✅ automated | integration: `tests/integration/team/lifecycle.test.ts` — deactivation (F12) restore does not silently bring back sensitive grants (T020) |
| T021 | Create project double click | Одна запись по idempotency key | ✅ automated | integration: `tests/integration/organization/projects.test.ts` — projects create is idempotent under double submit (T021)<br>e2e: `tests/e2e/specs/projects.spec.ts` — projects › create a project and open its workspace (F04 start, T021) |
| T022 | Project type change после episodes | Blocked с перечнем зависимостей | ✅ automated | integration: `tests/integration/organization/projects.test.ts` — projects locks the type once seasons exist (T022) |
| T023 | Archive active project | Нужна развязка scheduled publications/active shifts/open required work | ✅ automated | integration: `tests/integration/organization/projects.test.ts` — archiving an active project (T023) needs scheduled publications, active shifts and open high-priority work resolved first |
| T024 | Archive completed project | Историческая аналитика и finance сохраняются | ✅ automated | integration: `tests/integration/organization/projects.test.ts` — archiving a completed project (T024) keeps its historical analytics and finance readable and unchanged |
| T025 | Character new version | Old approved snapshot не меняется, dependent content отмечен для проверки | ✅ automated | integration: `tests/integration/creative/characters.test.ts` — characters approved snapshots never change; a new approved version flags dependent content (T025) |
| T026 | Season episode duplicate number/language | DB/domain unique conflict без потери исходной записи | ✅ automated | integration: `tests/integration/creative/series.test.ts` — series structure episode numbers are unique per season and language without losing the original (T026) |
| T027 | Reorder scenes | Stable IDs и связанные tasks/annotations остаются | ✅ automated | integration: `tests/integration/creative/series.test.ts` — series structure reordering scenes keeps stable ids and every linked record: characters, thumbnail file, the episode content and its tasks (T027) |
| T028 | Account duplicate URL with tracking params | Одна canonical identity, показ существующей разрешённой записи | ✅ automated | integration: `tests/integration/accounts/accounts.test.ts` — accounts: link-only registration duplicate URL with tracking parameters resolves to one canonical identity and shows the permitted record (T028)<br>unit: `packages/application/src/accounts/identity.test.ts` — profile URL identity tracking parameters, www/m hosts, trailing slashes and handle case never change an Instagram identity (T028) |
| T029 | Account custom case-sensitive path | Не склеивается ошибочно с другим path | ✅ automated | integration: `tests/integration/accounts/accounts.test.ts` — accounts: link-only registration custom platforms keep case-sensitive paths distinct (T029)<br>unit: `packages/application/src/accounts/identity.test.ts` — profile URL identity custom sites keep case-sensitive paths distinct (T029) |
| T030 | Account rename | История handle/URL и publications сохраняется | ✅ automated | integration: `tests/integration/accounts/accounts.test.ts` — accounts: link-only registration rename keeps the handle/URL history and publications stay linked (T030) |
| T031 | Account transfer с active shift | Операция blocked до разрешения open dependency | ✅ automated | integration: `tests/integration/accounts/accounts.test.ts` — accounts: transfer between projects is blocked while a shift is active (T031); afterwards old facts keep the old project and new facts get the new one (T032) |
| T032 | Account transfer после завершения | Старые facts имеют старый project attribution, новые — новый | ✅ automated | integration: `tests/integration/accounts/accounts.test.ts` — accounts: transfer between projects is blocked while a shift is active (T031); afterwards old facts keep the old project and new facts get the new one (T032) |
| T033 | Добавление Instagram URL | Нет OAuth, scraping, external fetch и fake imported metrics | ✅ automated | integration: `tests/integration/accounts/accounts.test.ts` — accounts: link-only registration adding an Instagram URL never fetches it and creates no statistics (T033) |
| T034 | Create reference from URL | Сохраняет заметку, не скачивает контент автоматически | ✅ automated | integration: `tests/integration/creative/references.test.ts` — references creating from a URL keeps a note and never downloads anything (T034) |
| T035 | Reference → Idea | Один linked draft, reference usage виден | ✅ automated | integration: `tests/integration/creative/references.test.ts` — references Use as Idea creates exactly one linked draft and shows the usage (T035)<br>integration: `tests/integration/production/content.test.ts` — content items (S22–S24, §10.1) Reference → Idea uses the content create use case: one linked draft, usage visible (T035) |
| T036 | Apply template повторно | Один набор tasks по application key | ✅ automated | integration: `tests/integration/production/content.test.ts` — content templates (T036, T037) applies a template exactly once and never overwrites completed tasks when a new template is applied<br>integration: `tests/integration/production/content.test.ts` — content templates (T036, T037) refuses a stale preview (tasks changed after the preview)<br>integration: `tests/integration/production/content.test.ts` — content templates (T036, T037) Create and Apply Template in one step<br>integration: `tests/integration/work/planning.test.ts` — task templates (T036) previews, then creates the graph exactly once per application key; unknown assignees stay Unassigned with a coordination task |
| T037 | Новый template к начатому content | Diff, completed tasks не перезаписаны | ✅ automated | integration: `tests/integration/production/content.test.ts` — content templates (T036, T037) applies a template exactly once and never overwrites completed tasks when a new template is applied<br>integration: `tests/integration/production/content.test.ts` — content templates (T036, T037) refuses a stale preview (tasks changed after the preview)<br>integration: `tests/integration/production/content.test.ts` — content templates (T036, T037) Create and Apply Template in one step |
| T038 | Submit version с Processing asset | Blocked до Available | ✅ automated | integration: `tests/integration/production/reviews.test.ts` — versions (§10.3) Submit is blocked while a file is still being checked, then allowed once Available (T038)<br>unit: `packages/application/src/production/rules.test.ts` — deliverables and submission a processing file blocks submission until it is available (T038)<br>unit: `packages/application/src/production/rules.test.ts` — deliverables and submission missing required slots, files not yet Available (uploading, checking, processing) or rejected and open mandatory items all block submission (T038) |
| T039 | Self approval при policy запрете | Forbidden; owner exception отдельный и audited | ✅ automated | integration: `tests/integration/production/reviews.test.ts` — reviews (S25, S26) forbids self-approval; the Owner exception is explicit and audited separately (T039)<br>unit: `packages/application/src/production/rules.test.ts` — review policy self-review is forbidden unless explicitly excepted (T039) |
| T040 | Review unresolved blocker | Approval blocked | ✅ automated | integration: `tests/integration/production/reviews.test.ts` — reviews (S25, S26) unresolved blocking comments block approval until resolved; reviewer can reopen (T040) |
| T041 | Параллельные approve/request changes | Одно решение, второй 412/409 по контракту | ✅ automated | integration: `tests/integration/production/reviews.test.ts` — reviews (S25, S26) concurrent approve / request changes: one decision, the other 412 or 409 (T041) |
| T042 | Approval неверной version | Не утверждает latest автоматически | ✅ automated | integration: `tests/integration/production/reviews.test.ts` — reviews (S25, S26) approving a version other than the reviewed one never approves the latest (T042) |
| T043 | New version после approval | Старые placements pinned, новая версия требует review | ✅ automated | integration: `tests/integration/production/reviews.test.ts` — reviews (S25, S26) a new revision after approval keeps the approved version pinned; the new version needs review (T043) |
| T044 | Revoke approval | Новые placements заблокированы, published history сохранена | ✅ automated | integration: `tests/integration/production/reviews.test.ts` — reviews (S25, S26) revoking approval blocks new placements and keeps published history with a flag and a check task (T044) |
| T045 | Video comment timecode вне duration | Validation error | ✅ automated | integration: `tests/integration/production/reviews.test.ts` — reviews (S25, S26) video timecodes are validated against the duration (T045) and image points stay on their version (T046)<br>unit: `packages/application/src/production/rules.test.ts` — annotations timecodes must lie within the duration (T045)<br>unit: `packages/application/src/production/rules.test.ts` — annotations unknown duration and non-timed files refuse a timecode (T045) |
| T046 | Image annotation после новой версии | Старые координаты не переносятся на новое изображение | ✅ automated | integration: `tests/integration/production/reviews.test.ts` — reviews (S25, S26) video timecodes are validated against the duration (T045) and image points stay on their version (T046) |
| T047 | Duplicate content | Нет inherited metrics, approvals, payouts и публикаций | ✅ automated | integration: `tests/integration/production/content.test.ts` — content items (S22–S24, §10.1) Duplicate as New Draft inherits no metrics, approvals, payouts, publications, versions or tasks (T047) |
| T048 | Task dependency cycle | Server rejects, graph остаётся ациклическим | ✅ automated | integration: `tests/integration/work/tasks.test.ts` — task lifecycle rejects self-dependencies and cycles; the graph stays acyclic (T048)<br>unit: `packages/application/src/work/rules/rules.test.ts` — dependency graph rejects self edges and cycles with the offending path (T048)<br>unit: `packages/application/src/work/rules/rules.test.ts` — dependency graph property: accepting only edges that do not close a cycle keeps the graph acyclic (T048)<br>unit: `packages/application/src/work/rules/rules.test.ts` — dependency graph property: createsCycle agrees with a brute-force acyclicity check (T048) |
| T049 | Start blocked task | Объяснение predecessor или explicit audited override | ✅ automated | integration: `tests/integration/work/tasks.test.ts` — task lifecycle start of a task with an unfinished predecessor is explained, or overridden by a lead with reason and audit (T049) |
| T050 | Complete с required checklist unchecked | Server rejects | ✅ automated | integration: `tests/integration/work/tasks.test.ts` — task lifecycle completion is rejected while mandatory checklist items are open (T050) |
| T051 | Reopen Done | Новый cycle event, не повторный produced unit | ✅ automated | integration: `tests/integration/work/tasks.test.ts` — task lifecycle reopen creates a new cycle event; the task is not counted again as a produced unit (T051)<br>unit: `packages/application/src/work/rules/rules.test.ts` — task status rules classifies transitions and starts a new cycle on reopen (T051) |
| T052 | Task без deadline | Не попадает в overdue | ✅ automated | unit: `packages/application/src/work/rules/rules.test.ts` — task status rules overdue is computed and a task without deadline is never overdue (T052) |
| T053 | Due date-only в разных zones | Одинаковый сохранённый deadline, корректный local display | ✅ automated | integration: `tests/integration/work/tasks.test.ts` — task create/edit a date-only deadline is the end of that day in the task zone and the same stored moment for everyone (T053)<br>unit: `packages/application/src/work/rules/rules.test.ts` — deadlines (T053) date-only deadline is the end of the chosen day in the task zone, identical for every viewer<br>unit: `packages/application/src/work/rules/rules.test.ts` — deadlines (T053) shifting a date-only deadline keeps end of day across a DST change |
| T054 | Recurrence retry после outage | Нет duplicate occurrences и неконтролируемого backfill | ✅ automated | integration: `tests/integration/work/planning.test.ts` — recurring tasks generates occurrences within the horizon with unique keys; a retried run after an outage creates no duplicates and one overdue occurrence (T054)<br>unit: `packages/application/src/work/rules/rules.test.ts` — recurrence after an outage only one overdue occurrence is created and the rest are listed as missed (T054) |
| T055 | Monthly recurrence 31st | Last Day policy воспроизводима | ✅ automated | integration: `tests/integration/work/planning.test.ts` — recurring tasks monthly on the 31st follows the Last Day of Month policy reproducibly (T055)<br>unit: `packages/application/src/work/rules/rules.test.ts` — recurrence monthly on the 31st uses the last day of shorter months, reproducibly (T055) |
| T056 | Timer start в двух вкладках | Один active timer | ✅ automated | integration: `tests/integration/work/time.test.ts` — timers allows one running timer per member even from two tabs (T056) and one entry per stop replay (T057) |
| T057 | Timer stop replay | Один TimeEntry | ✅ automated | integration: `tests/integration/work/time.test.ts` — timers allows one running timer per member even from two tabs (T056) and one entry per stop replay (T057) |
| T058 | Browser closed при timer | Server interval сохраняется, нет fake auto-stop | ✅ automated | integration: `tests/integration/work/time.test.ts` — timers keeps the server interval when the browser is gone: no invented stop, Needs Review after 12 h (T058) |
| T059 | Overlapping time entries | Не проходят approval без resolution | ✅ automated | integration: `tests/integration/work/time.test.ts` — manual entries and timesheets overlapping entries are flagged and cannot be submitted or approved until resolved (T059)<br>unit: `packages/application/src/work/rules/rules.test.ts` — time entries detects overlapping intervals but not touching ones (T059) |
| T060 | Unestimated workload | Показывается unknown count, не нулевые часы | ✅ automated | integration: `tests/integration/work/workload.test.ts` — workload (S29) unestimated tasks are an explicit unknown count, never zero hours; capacity is unknown until confirmed (T060)<br>unit: `packages/application/src/work/rules/rules.test.ts` — workload spreads remaining estimates over available working days; unestimated is a count, not zero hours (T060)<br>unit: `packages/application/src/work/rules/rules.test.ts` — workload capacity that is not set stays unknown (never assumed) (T060) |
| T061 | Schedule unapproved content | Blocked | ✅ automated | integration: `tests/integration/publishing/publications.test.ts` — publications: scheduling gates blocks scheduling of unapproved or revoked content, while a draft may be saved (T061) |
| T062 | Schedule account Restricted | Только разрешённый override с reason | ✅ automated | integration: `tests/integration/publishing/publications.test.ts` — publications: scheduling gates allows a Restricted account only with a lead override and a reason; archived accounts never (T062) |
| T063 | Наступил scheduled_at | Publication не становится Published сама | ✅ automated | integration: `tests/integration/publishing/publications.test.ts` — publications: confirmation never becomes Published because time passed; a reminder asks to confirm (T063) |
| T064 | Mark Published без URL/reason | Validation error | ✅ automated | integration: `tests/integration/publishing/publications.test.ts` — publications: confirmation requires the post URL or a 10–500 character reason (T064) |
| T065 | Mark Published дважды | Один факт и один набор checkpoints | ✅ automated | integration: `tests/integration/publishing/publications.test.ts` — publications: confirmation creates exactly one fact, one set of checkpoints and measurement tasks, even when repeated (T065) |
| T066 | External Post URL duplicate | Unique conflict даже при разных idempotency keys | ✅ automated | integration: `tests/integration/publishing/publications.test.ts` — publications: confirmation rejects the same post URL on another placement even with another idempotency key (T066) |
| T067 | План перенесён за пределы недели | Original baseline сохраняет строку, Current Plan отдельно | ✅ automated | integration: `tests/integration/publishing/plan-calendar.test.ts` — plan baselines keeps the original weekly row when a placement moves out of the frozen week; Current Plan is separate (T067) |
| T068 | Late metrics checkpoint | Реальный observed_at, label Late, исключён из стандартного comparable set | ✅ automated | integration: `tests/integration/insights/checkpoints.test.ts` — Metrics Inbox and checkpoints (S49) keeps the real observed time of a late checkpoint entry and excludes it from the standard window (T068)<br>integration: `tests/integration/publishing/experiments.test.ts` — experiments stores the actual published and observed times so a late checkpoint is labelled Late and left out of the comparable set (T068)<br>unit: `packages/analytics/src/observations.test.ts` — checkpoint timing and selection labels late observations with their real time and keeps them out of the comparable set (T068) |
| T069 | Removed external post | Исторические facts сохранены, availability отдельно | ✅ automated | integration: `tests/integration/publishing/publications.test.ts` — publications: confirmation records removed/unavailable posts without deleting facts (T069) |
| T070 | Tagged URL created | Нет увеличения clicks/conversions | ✅ automated | integration: `tests/integration/publishing/campaigns.test.ts` — campaigns builds tagged links without clicks; results come only from entered source reports (T070) |
| T071 | Campaign multi-project costs | Сумма allocations ровно source amount, tags не удваивают расход | ✅ automated | integration: `tests/integration/publishing/campaigns.test.ts` — campaigns allocates a shared campaign cost exactly across projects; tags never double count (T071)<br>unit: `packages/application/src/publishing/logic.test.ts` — campaign cost split (T071) always sums exactly to the source amount (property)<br>unit: `packages/application/src/publishing/logic.test.ts` — campaign cost split (T071) splits equally with deterministic remainders and validates exact amounts |
| T072 | Experiment unequal post ages | Not Comparable/фильтр, без ложного winner significance | ✅ automated | integration: `tests/integration/publishing/experiments.test.ts` — experiments marks unequal post ages Not Comparable and never names a winner or significance (T072)<br>unit: `packages/application/src/publishing/logic.test.ts` — comparable results compares only values observed at the same post age (T072)<br>unit: `packages/application/src/publishing/logic.test.ts` — comparable results never produces a comparable status when a variant has no comparable value (T072) |
| T073 | MIME spoofed upload | Quarantine rejection, нет публичного preview | ✅ automated | integration: `tests/integration/media/uploads.test.ts` — upload pipeline (section 14) rejects a file whose content does not match an allowed type (T073) |
| T074 | Image decompression/size limit | Reject до опасного processing, reservation освобождена | ✅ automated | integration: `tests/integration/media/library.test.ts` — upload pipeline additions rejects an avatar over the decoded pixel limit and releases the reservation (T074) |
| T075 | Interrupted multipart upload | Resume в пределах TTL, нет дублирующего blob ownership | ✅ automated | integration: `tests/integration/media/library.test.ts` — upload pipeline additions resumes an interrupted multipart upload within the TTL into one file with one stored blob; after the TTL it expires cleanly (T075) |
| T076 | Two uploads near quota | Concurrent reservations не превышают quota | ✅ automated | integration: `tests/integration/media/uploads.test.ts` — upload pipeline (section 14) concurrent reservations cannot exceed the quota (T076) |
| T077 | Malware scan service unavailable | Checking/Failed, файл не Available | ✅ automated | integration: `tests/integration/media/library.test.ts` — upload pipeline additions keeps the file Checking while the malware scanner is down and fails it honestly after the last attempt (T077) |
| T078 | Private original URL запрошен без права | Нет credentials и metadata leak | ✅ automated | integration: `tests/integration/media/library.test.ts` — restricted media (§6, T078, T079) restricted files are invisible without restricted access: list, detail, thumbnail, search, export (T078)<br>integration: `tests/integration/media/library.test.ts` — restricted media (§6, T078, T079) revoking restricted access stops new reads through the proxy immediately (T079)<br>integration: `tests/integration/media/uploads.test.ts` — upload pipeline (section 14) a member without project access cannot see or download another project’s file (T078) |
| T079 | Restricted asset revoke | Новые proxy reads denied, downloaded original не обещается отозвать | ✅ automated | integration: `tests/integration/media/library.test.ts` — restricted media (§6, T078, T079) restricted files are invisible without restricted access: list, detail, thumbnail, search, export (T078)<br>integration: `tests/integration/media/library.test.ts` — restricted media (§6, T078, T079) revoking restricted access stops new reads through the proxy immediately (T079) |
| T080 | Delete referenced approved version | Blocked или Archive, historical playback reference сохранён | ✅ automated | integration: `tests/integration/media/library.test.ts` — versions (S37): new version, delete guard for referenced versions (T080), purge refuses to delete a referenced version, offers archive, keeps the historical file; unreferenced versions are purged later |
| T081 | External asset inaccessible | Honest External Link, не fake local file | ✅ automated | integration: `tests/integration/media/library.test.ts` — library listing, filters and scope shows an external link honestly: metadata only, no download, no preview (T081) |
| T082 | Article published | Frozen version, новая правка draft | ✅ automated | integration: `tests/integration/knowledge/articles.test.ts` — knowledge articles: publish, frozen versions, drafts publishing freezes the version; a later edit becomes a new draft and never changes the published text (T082) |
| T083 | Article opened without acknowledge | Required reading не выполнено | ✅ automated | integration: `tests/integration/knowledge/articles.test.ts` — required reading and acknowledgements opening the article does not acknowledge it; only the explicit action does, idempotently (T083) |
| T084 | Required article major revision | Новый acknowledgement request, прежний факт сохранён | ✅ automated | integration: `tests/integration/knowledge/articles.test.ts` — required reading and acknowledgements a major revision of required reading asks again and keeps the earlier acknowledgement; a minor one does not (T084) |
| T085 | Overlapping scheduled member shifts | Conflict, кроме единой multi-account Shift | ✅ automated | integration: `tests/integration/ofm/schedule.test.ts` — Shift schedule (S42) T085: a member cannot hold overlapping shifts, except as one multi-account shift |
| T086 | Multi-account Shift | Один timer, account reports раздельны, время не умножено | ✅ automated | integration: `tests/integration/ofm/schedule.test.ts` — Shift schedule (S42) T086: a multi-account shift runs one timer, keeps account reports separate and counts its hours once |
| T087 | Shift вне assignment interval | Start/schedule blocked | ✅ automated | integration: `tests/integration/ofm/schedule.test.ts` — Shift schedule (S42) T087: shifts outside the assignment interval are blocked |
| T088 | Start Shift из двух вкладок | Один actual_start и одна Active Shift | ✅ automated | integration: `tests/integration/ofm/lifecycle.test.ts` — Shift lifecycle (S43, §13.2) T088: starting from two tabs yields one actual start and one active shift |
| T089 | Shift Pause/Resume/End | Breaks закрыты ровно раз, Net Hours верны | ✅ automated | integration: `tests/integration/ofm/lifecycle.test.ts` — Shift lifecycle (S43, §13.2) T089: pause/resume/end close every break exactly once and net hours exclude breaks<br>unit: `packages/domain/src/ofm.test.ts` — net hours (M28, T089) subtracts closed breaks exactly once<br>unit: `packages/domain/src/ofm.test.ts` — net hours (M28, T089) is Pending (null), never 0, without an actual end (T090)<br>unit: `packages/domain/src/ofm.test.ts` — net hours (M28, T089) property: net = gross − disjoint breaks, never negative, running timer agrees at the end<br>+1 more |
| T090 | Forgotten End | Alert/Needs Review, actual_end не выдуман | ✅ automated | integration: `tests/integration/ofm/lifecycle.test.ts` — Shift lifecycle (S43, §13.2) T090: a forgotten End raises a supervisor alert and Needs Review without inventing an actual end<br>unit: `packages/domain/src/ofm.test.ts` — net hours (M28, T089) is Pending (null), never 0, without an actual end (T090) |
| T091 | Correct shift time | Supervisor reason и audit, compensation source invalidation | ✅ automated | integration: `tests/integration/ofm/lifecycle.test.ts` — Shift lifecycle (S43, §13.2) T091: supervisor time correction needs a reason, is audited and flags compensation sources |
| T092 | Submit report без handover/no-open-items | Validation error | ✅ automated | integration: `tests/integration/ofm/reports-handovers.test.ts` — Shift reports (§13.3) T092: submit requires a summary and handover items or an explicit No Open Items |
| T093 | Report approved | Версия frozen, finance entry не Post автоматически | ✅ automated | integration: `tests/integration/ofm/reports-handovers.test.ts` — Shift reports (§13.3) T093: approval freezes the version and never posts finance; changes requested produce a new version |
| T094 | Handover acknowledged | Задачи не завершаются и не клонируются | ✅ automated | integration: `tests/integration/ofm/reports-handovers.test.ts` — Handovers (S44) routes to the next shift, requires acknowledgement at start, and acknowledgement never completes or clones tasks (T094) |
| T095 | Same contact alias on two accounts | Разные contacts, нет автоматического identity merge | ✅ automated | integration: `tests/integration/ofm/contacts.test.ts` — OFM contacts (S45/S46) T095: the same alias on two accounts is two contacts; duplicates per account are refused; no cross-account merge |
| T096 | Merge same-account contacts | Все refs сохранены, sales не дублируются | ✅ automated | integration: `tests/integration/ofm/contacts.test.ts` — OFM contacts (S45/S46) T096: merging same-account contacts keeps every reference and never duplicates sales |
| T097 | Content request из contact | Creator получает brief, не private contact notes | ✅ automated | integration: `tests/integration/ofm/operations-sales.test.ts` — Operations Queue (S47) T097: a content request gives the creator a brief task without private contact notes or history |
| T098 | Sale candidate совпадает с source transaction | Duplicate detected, revenue не удвоен | ✅ automated | integration: `tests/integration/finance/reconciliation.test.ts` — sale candidate reconciliation (T098) verifies into a draft, detects duplicates of recorded sources, never posts<br>integration: `tests/integration/ofm/operations-sales.test.ts` — Sale candidates (§13.5) T098: a duplicate source transaction is detected against candidates and finance; revenue is never doubled |
| T099 | Sale совпадает по времени со сменой | Attribution остаётся Unassigned без evidence/ручного решения | ✅ automated | integration: `tests/integration/ofm/operations-sales.test.ts` — Sale candidates (§13.5) T099: a sale during a shift stays Unassigned without an explicit allocation; shares never exceed 100%<br>unit: `packages/domain/src/ofm.test.ts` — sale attribution (T099) no shares → Unassigned 100 %<br>unit: `packages/domain/src/ofm.test.ts` — sale attribution (T099) 60/40 is full; shares above 100 % are rejected<br>unit: `packages/domain/src/ofm.test.ts` — sale attribution (T099) property: unassigned = 100 − Σ shares whenever valid |
| T100 | Quality all N/A | No Score, не 0/100 | ✅ automated | integration: `tests/integration/ofm/quality.test.ts` — Quality reviews (S48, §13.6) T100: all criteria Not Applicable → No Score (never 0 or 100)<br>unit: `packages/domain/src/ofm.test.ts` — quality score (M31) all Not Applicable → No Score, never 0 or 100 (T100) |
| T101 | Quality negative без evidence | Publish blocked | ✅ automated | integration: `tests/integration/ofm/quality.test.ts` — Quality reviews (S48, §13.6) T101: negative scores need evidence before publishing; every criterion must be answered<br>unit: `packages/domain/src/ofm.test.ts` — quality score (M31) negative scores need evidence (T101) |
| T102 | Quality disputed | Исходная оценка и resolution history сохранены | ✅ automated | integration: `tests/integration/ofm/quality.test.ts` — Quality reviews (S48, §13.6) T102: a dispute keeps the original score and a resolution history; a revision replaces it |
| T103 | Metric empty и metric zero | null и 0 различаются в store/chart/export | ✅ automated | integration: `tests/integration/insights/observations.test.ts` — metric observations (S50) keeps an unknown value distinct from a reported zero in the store, analytics and export (T103)<br>unit: `packages/analytics/src/formulas.test.ts` — metric formulas distinguishes empty from zero (T103)<br>unit: `packages/analytics/src/observations.test.ts` — grouping fills series: counts are known zero, observed values are gaps (T103) |
| T104 | Cumulative snapshots 100→160 | Delta 60, cumulative total не 260 | ✅ automated | integration: `tests/integration/insights/semantic.test.ts` — semantic layer: publications and cumulative observations turns cumulative snapshots 100 → 160 into a delta of 60, never a total of 260 (T104)<br>unit: `packages/analytics/src/formulas.test.ts` — metric formulas cumulative snapshots give deltas, not sums (T104) and flag negative deltas (T107) |
| T105 | Overlapping period observations | Не суммируются, нужен canonical non-overlap набор | ✅ automated | integration: `tests/integration/insights/semantic.test.ts` — semantic layer: accounts, periods and OFM never sums overlapping periods until one of them is excluded from reports (T105)<br>unit: `packages/analytics/src/observations.test.ts` — non-overlapping period selection (T105) never sums overlapping periods; adjacent periods are fine<br>unit: `packages/analytics/src/observations.test.ts` — non-overlapping period selection (T105) exact duplicates are conflicts as well<br>unit: `packages/analytics/src/observations.test.ts` — non-overlapping period selection (T105) included periods never overlap each other (property)<br>+1 more |
| T106 | Metric correction approved | Старое значение superseded, graph пересчитан из нового источника | ✅ automated | integration: `tests/integration/insights/observations.test.ts` — metric observations (S50) applies an approved correction as a new revision and recomputes metrics from it (T106) |
| T107 | Negative cumulative delta | Source correction warning, не ложные отрицательные просмотры | ✅ automated | integration: `tests/integration/insights/semantic.test.ts` — semantic layer: publications and cumulative observations flags a lower cumulative total as a source correction instead of negative views (T107)<br>unit: `packages/analytics/src/formulas.test.ts` — metric formulas cumulative snapshots give deltas, not sums (T104) and flag negative deltas (T107) |
| T108 | ER при views=0 | Not Defined, division by zero отсутствует | ✅ automated | integration: `tests/integration/insights/semantic.test.ts` — semantic layer: publications and cumulative observations computes ER per publication with Not Defined at zero views, lists missing fields and weights the aggregate (T108, T109, T110)<br>unit: `packages/analytics/src/formulas.test.ts` — metric formulas ER with zero views is not defined (T108); ER may exceed 100 |
| T109 | Partial interactions | Не выдаётся полный ER, missing fields перечислены | ✅ automated | integration: `tests/integration/insights/semantic.test.ts` — semantic layer: publications and cumulative observations computes ER per publication with Not Defined at zero views, lists missing fields and weights the aggregate (T108, T109, T110)<br>unit: `packages/analytics/src/formulas.test.ts` — metric formulas partial interactions list missing fields (T109) |
| T110 | Aggregate ER | Weighted ratio, не average percentages | ✅ automated | integration: `tests/integration/insights/semantic.test.ts` — semantic layer: publications and cumulative observations computes ER per publication with Not Defined at zero views, lists missing fields and weights the aggregate (T108, T109, T110)<br>unit: `packages/analytics/src/formulas.test.ts` — metric formulas aggregate ER is a weighted ratio, not an average of percentages (T110) |
| T111 | Followers first snapshot=0 | Absolute growth показан, relative undefined | ✅ automated | integration: `tests/integration/insights/semantic.test.ts` — semantic layer: accounts, periods and OFM shows the absolute follower change when the first snapshot is 0 and no relative growth (T111)<br>unit: `packages/analytics/src/formulas.test.ts` — metric formulas growth from a zero first snapshot is not defined (T111)<br>unit: `packages/analytics/src/observations.test.ts` — snapshot change (M11/M12) uses first and last usable snapshots and keeps absolute growth when first = 0 (T111) |
| T112 | Source не даёт churn denominator | Not Measured, нет подстановки текущих subscribers | ✅ automated | integration: `tests/integration/insights/semantic.test.ts` — semantic layer: accounts, periods and OFM reports churn as Not Measured without a starting cohort and never substitutes current subscribers (T112) |
| T113 | Account followers across platforms | Label Sum of Account Followers, не Unique Audience | ✅ automated | integration: `tests/integration/insights/semantic.test.ts` — semantic layer: accounts, periods and OFM labels followers across platforms as a Sum of Account Followers, not unique audience (T113) |
| T114 | Coverage Missing checkpoint | Missing не входит в usable numerator | ✅ automated | integration: `tests/integration/insights/checkpoints.test.ts` — Metrics Inbox and checkpoints (S49) closes a checkpoint as Missing without creating values; Missing is expected but never usable (T114)<br>unit: `packages/analytics/src/observations.test.ts` — completeness and coverage missing checkpoints are expected but never usable (T114) |
| T115 | Report task-comments join | Facts не размножаются от нескольких comments | ✅ automated | integration: `tests/integration/insights/reports.test.ts` — Report Builder (S52) aggregates each fact to the grain before joining: task comments never multiply time (T115) |
| T116 | Shared report restrictive recipient | Recipient-scoped results, broad owner data не отправлено | ✅ automated | integration: `tests/integration/insights/reports.test.ts` — Report Builder (S52) delivers scheduled reports per recipient with their own permissions into their Inbox (T116) |
| T117 | Unfinished period comparison | Equal elapsed window, rate delta в percentage points | ✅ automated | integration: `tests/integration/insights/semantic.test.ts` — semantic layer: comparison, scope and finance compares an unfinished period with the same elapsed window and rates in percentage points (T117)<br>unit: `packages/analytics/src/formulas.test.ts` — periods and comparison compares an unfinished month with the same elapsed window (T117) |
| T118 | Goal revised mid-period | История target и baseline сохранена | ✅ automated | integration: `tests/integration/goals/goals.test.ts` — goals (S53, §17) a target revised mid-period keeps the target and baseline history (T118) |
| T119 | Finance example 1000/100/180/200/72 | Net 720, result 448, как в 18.7 | ✅ automated | integration: `tests/integration/finance/worked-example.test.ts` — finance control example (§18.7) reproduces 720 / 448 / 42 / 490; a replayed compensation approve leaves one expense document and one claim (T119, T120, T121, T130)<br>unit: `packages/domain/src/finance/finance.test.ts` — ledger arithmetic (§18.2, §18.7) reproduces the control example: net 720, result 448 (T119) |
| T120 | Platform payout 720 | Cash увеличен, revenue/result не изменены | ✅ automated | integration: `tests/integration/finance/worked-example.test.ts` — finance control example (§18.7) reproduces 720 / 448 / 42 / 490; a replayed compensation approve leaves one expense document and one claim (T119, T120, T121, T130) |
| T121 | Partial manager payout 30 | Outstanding 42, compensation expense не дублируется | ✅ automated | integration: `tests/integration/finance/worked-example.test.ts` — finance control example (§18.7) reproduces 720 / 448 / 42 / 490; a replayed compensation approve leaves one expense document and one claim (T119, T120, T121, T130) |
| T122 | Post одной transaction concurrent keys | Unique source guard, одна экономическая операция | ✅ automated | integration: `tests/integration/finance/ledger.test.ts` — financial arithmetic rules one source transaction cannot be recorded twice, even concurrently (T122) |
| T123 | Edit Posted entry | Запрещено, доступен reversal/replacement | ✅ automated | integration: `tests/integration/finance/ledger.test.ts` — financial entries: lifecycle and immutability posted entries are immutable; reversal and replacement are offered (T123) |
| T124 | Gross неизвестен, Net Only известен | Net valid, Gross incomplete, fee не выдумана | ✅ automated | integration: `tests/integration/finance/ledger.test.ts` — financial arithmetic rules net-only statements: net valid, gross incomplete, no invented fee (T124)<br>unit: `packages/domain/src/finance/finance.test.ts` — ledger arithmetic (§18.2, §18.7) net-only statements keep net valid and mark gross incomplete, fees not invented (T124) |
| T125 | Header platform total + transaction lines | Header контрольный, не вторая выручка | ✅ automated | integration: `tests/integration/finance/ledger.test.ts` — financial arithmetic rules header total is a control sum, never a second revenue line (T125)<br>unit: `packages/domain/src/finance/finance.test.ts` — ledger arithmetic (§18.2, §18.7) the statement header total is a control sum, not revenue (T125) |
| T126 | FX missing | Draft save возможен, cross-currency Post blocked | ✅ automated | integration: `tests/integration/finance/ledger.test.ts` — financial arithmetic rules missing FX keeps the draft but blocks cross-currency posting (T126); used rates are frozen (T128) |
| T127 | Allocation rounding remainder | Exact minor-unit conservation | ✅ automated | integration: `tests/integration/finance/ledger.test.ts` — financial arithmetic rules allocation rounding conserves minor units exactly (T127) and posted re-allocation is an adjustment<br>unit: `packages/domain/src/finance/finance.test.ts` — allocation (§18.3, T127) rejects remainders unless explicitly Unallocated<br>unit: `packages/domain/src/finance/finance.test.ts` — allocation (§18.3, T127) percentages must sum to exactly 100<br>unit: `packages/domain/src/finance/finance.test.ts` — allocation (§18.3, T127) splits 100.00 in three equal parts exactly with a deterministic tie<br>+5 more |
| T128 | Approved FX rate изменён в справочнике | Историческая base equivalent неизменна | ✅ automated | integration: `tests/integration/finance/ledger.test.ts` — financial arithmetic rules missing FX keeps the draft but blocks cross-currency posting (T126); used rates are frozen (T128) |
| T129 | Commitment converted to Actual | Remaining commitment уменьшен, расходов дважды нет | ✅ automated | integration: `tests/integration/finance/budgets.test.ts` — budgets and commitments a commitment converted to actual is never counted twice (T129) |
| T130 | Compensation run replay approve | Один expense document и один entitlement claim | ✅ automated | integration: `tests/integration/finance/compensation.test.ts` — compensation rules and runs an entitlement is claimed by one approved run only; a replayed approve adds no document or claim; manual compensation expenses must be reconciled (T130)<br>integration: `tests/integration/finance/worked-example.test.ts` — finance control example (§18.7) reproduces 720 / 448 / 42 / 490; a replayed compensation approve leaves one expense document and one claim (T119, T120, T121, T130) |
| T131 | Compensation refund after approval | Adjustment next open run, прошлый paid run не переписан | ✅ automated | integration: `tests/integration/finance/compensation.test.ts` — compensation rules and runs refund after approval → adjustment in the next run; negative balance carried forward (T131, T135) |
| T132 | Fixed mid-month proration | Выбранная Calendar Days/None policy воспроизводима | ✅ automated | integration: `tests/integration/finance/compensation.test.ts` — compensation rules and runs fixed mid-month proration: Calendar Days and None are reproducible (T132)<br>unit: `packages/domain/src/finance/finance.test.ts` — compensation (§18.6) fixed mid-month proration: Calendar Days vs None are reproducible (T132) |
| T133 | Hourly overlapping sources | TimeEntry и Shift одного периода не оплачены дважды одним basis | ✅ automated | integration: `tests/integration/finance/compensation.test.ts` — compensation rules and runs hourly TimeEntry and Shift time of the same period is paid once (T133)<br>unit: `packages/domain/src/finance/finance.test.ts` — compensation (§18.6) hourly: TimeEntry and Shift of the same period are paid once (T133) |
| T134 | Revenue Share overlapping rules | Ошибка либо explicit stack preview, нет скрытого двойного начисления | ✅ automated | integration: `tests/integration/finance/compensation.test.ts` — compensation rules and runs overlapping revenue shares: error or explicit stack preview (T134)<br>unit: `packages/domain/src/finance/finance.test.ts` — compensation (§18.6) overlapping rules: same component blocked, others need a stack group, stacked shares ≤ 100 % (T134) |
| T135 | Negative compensation balance | Carry-forward, нет автоматического банковского списания | ✅ automated | integration: `tests/integration/finance/compensation.test.ts` — compensation rules and runs refund after approval → adjustment in the next run; negative balance carried forward (T131, T135)<br>unit: `packages/domain/src/finance/finance.test.ts` — compensation (§18.6) negative recipient totals are carried forward, never collected (T135) |
| T136 | Closed-period post | Blocked или explicit audited reopen | ✅ automated | integration: `tests/integration/finance/ledger.test.ts` — financial arithmetic rules closed periods block posting; audited reopen allows it (T136) |
| T137 | Settlement overpayment | Явный advance/unallocated balance | ✅ automated | integration: `tests/integration/finance/ledger.test.ts` — settlements overpayment needs an explicit advance/unallocated balance (T137) and matches later |
| T138 | Deal Won | Не создаёт fake Paid/posted income | ✅ automated | integration: `tests/integration/finance/reconciliation.test.ts` — deals and campaign panels (T138, T016) a won deal creates no income; members without finance rights see no amounts<br>integration: `tests/integration/partners/deals.test.ts` — deals Won never creates paid or posted income (T138) |
| T139 | Automation Dry Run | Zero domain mutations и zero mail | ✅ automated | integration: `tests/integration/automation/execution.test.ts` — automation execution (§19) dry run evaluates and previews with zero domain mutations and zero mail (T139) |
| T140 | Worker retry после commit/response loss | Один observable domain effect | ✅ automated | integration: `tests/integration/automation/execution.test.ts` — automation execution (§19) a lost result or a duplicate delivery produces one observable effect; Retry keeps the operation key (T140) |
| T141 | Automation recursive chain | Depth/budget limit, alert без бесконечной генерации | ✅ automated | integration: `tests/integration/automation/execution.test.ts` — automation execution (§19) stops recursive chains by rule repetition, depth and budget with an alert (T141)<br>unit: `packages/domain/src/automation.test.ts` — automation chain limits stops at depth, recursion and budget (T141)<br>unit: `packages/domain/src/automation.test.ts` — automation chain limits throttles above the hourly rate without dropping runs (T141) |
| T142 | Rule owner loses scope | Paused Requires Attention, no unauthorized action | ✅ automated | integration: `tests/integration/automation/execution.test.ts` — automation execution (§19) pauses as Requires Attention when the owner loses scope, without any unauthorized action (T142)<br>integration: `tests/integration/automation/execution.test.ts` — automation execution (§19) revalidates proactively when an owner is removed from the project team (T142) |
| T143 | Rescheduled reminder | Stale deadline revision ничего не отправляет | ✅ automated | integration: `tests/integration/work/my-work.test.ts` — due reminders a rescheduled deadline makes the old reminder stale: it never sends (T143); snooze never changes the deadline |
| T144 | Quiet hours | Inbox immediate, обычный email отложен, security policy сохранена | ✅ automated | integration: `tests/integration/automation/execution.test.ts` — automation execution (§19) notifications are in the Inbox immediately; email copies follow the quiet-hours policy (T144)<br>integration: `tests/integration/platform/inbox.test.ts` — quiet hours and digest (T144) creates the inbox record immediately, defers ordinary e-mail and never defers security alerts<br>integration: `tests/integration/platform/inbox.test.ts` — quiet hours and digest (T144) sends the opted-in daily digest once per local day, after quiet hours, with a count only<br>unit: `packages/application/src/platform/platform-pure.test.ts` — daily digest timing (T144) is never due inside quiet hours and is due once the quiet period ended |
| T145 | Import validation error | Никакие domain rows не изменены | ✅ automated | integration: `tests/integration/platform/imports.test.ts` — Import Center — projects dataset a validation error blocks Confirm and changes no domain rows (T145) |
| T146 | Import target изменился после preview | Needs Revalidation, zero partial commit | ✅ automated | integration: `tests/integration/platform/imports.test.ts` — Import Center — projects dataset targets changed after validation → Needs Revalidation with zero partial commit (T146) |
| T147 | Import same file confirmed twice | Один import application | ✅ automated | integration: `tests/integration/platform/imports.test.ts` — Import Center — projects dataset parses CSV (BOM, semicolons), validates, commits atomically exactly once (T147) |
| T148 | Finance import | Только Draft, нет auto-post | ✅ automated | integration: `tests/integration/finance/reconciliation.test.ts` — import and export datasets financial import creates drafts only — never posted — validates references and duplicates, undo removes untouched drafts (T148) |
| T149 | Import ambiguous date/decimal | Требуется mapping, нет угадывания суммы | ✅ automated | integration: `tests/integration/platform/imports.test.ts` — Import Center — typed columns and XLSX never guesses ambiguous numbers or dates; an explicit format resolves them (T149)<br>unit: `packages/application/src/platform/imports/imports.test.ts` — value coercion never guesses grouping separators: an ambiguous amount needs the separator mapping (T149)<br>unit: `packages/application/src/platform/imports/imports.test.ts` — value coercion with the chosen separator every amount is read exactly, never rescaled (T149)<br>unit: `packages/application/src/platform/imports/imports.test.ts` — value coercion accepts ISO dates or the explicitly chosen format only; an ambiguous date is not guessed (T149) |
| T150 | Undo import после зависимых изменений | Препятствия видны, связанные факты не удалены | ✅ automated | integration: `tests/integration/platform/imports.test.ts` — Import Center — projects dataset Undo Import shows obstacles and never deletes related facts (T150) |
| T151 | CSV formula injection export | Открывается как безопасный текст | ✅ automated | integration: `tests/integration/platform/exports.test.ts` — Export Center (S54) neutralises spreadsheet formulas in CSV and XLSX without breaking numeric columns (T151)<br>unit: `packages/application/src/platform/platform-pure.test.ts` — spreadsheet export safety (T151) neutralises formula-like text but keeps typed numbers numeric<br>unit: `packages/application/src/platform/platform-pure.test.ts` — spreadsheet export safety (T151) no neutralised text cell ever starts with a formula trigger<br>unit: `packages/application/src/platform/platform-pure.test.ts` — spreadsheet export safety (T151) quotes CSV fields per RFC 4180 |
| T152 | XLSX formula/macro import | Ничего не выполняется, cached values явно обозначены | ✅ automated | integration: `tests/integration/platform/imports.test.ts` — Import Center — typed columns and XLSX XLSX formulas are never evaluated; cached values need confirmation and are labelled (T152)<br>unit: `packages/application/src/platform/imports/imports.test.ts` — XLSX parsing (T152) reads cached formula results only and flags formulas without a stored value<br>unit: `packages/application/src/platform/imports/imports.test.ts` — XLSX parsing (T152) parses a workbook without evaluating anything<br>unit: `packages/application/src/platform/imports/imports.test.ts` — XLSX parsing (T152) rejects macro-enabled content |
| T153 | Export permission revoked before download | Download denied и прежний artifact недоступен через новый credential | ✅ automated | integration: `tests/integration/platform/exports.test.ts` — Export Center (S54) re-checks access at download time; a revoked member cannot use old or new links (T153) |
| T154 | Export failed | Нет пустого completed файла | ✅ automated | integration: `tests/integration/platform/exports.test.ts` — Export Center (S54) a failed export never yields an empty "completed" file and can be retried (T154) |
| T155 | Restore archived/trashed record collision | Preview и resolution, без нарушения unique constraints | ✅ automated | integration: `tests/integration/platform/archive.test.ts` — Archive / Trash (S70) restoring an archived direction whose name is taken needs an explicit rename: previewed, refused without it, never skipped (T155)<br>integration: `tests/integration/platform/archive.test.ts` — Archive / Trash (S70) restoring an archived folder next to a same-named active folder needs an explicit rename (T155) |
| T156 | Purge finance/audit через trash | Forbidden | ✅ automated | integration: `tests/integration/platform/archive.test.ts` — Archive / Trash (S70) finance and audit records can never be purged through the trash, even by the Owner (T156) |
| T157 | Backup восстановлен до удаления контакта | Tombstones replay удаляет/обезличивает контакт до reopening | ✅ automated | integration: `tests/integration/ofm/contacts.test.ts` — OFM contacts (S45/S46) T157: a database restored to before an erasure is corrected by replaying the tombstone journal before reopening |
| T158 | Restore drill | Сверены DB/file manifests, result recorded, RTO измерен | ✅ automated | integration: `tests/integration/platform/restore-drill.test.ts` — backup and restore drill (T158) restores the latest encrypted dump into an isolated database, replays tombstones and records the measured result |
| T159 | Search чужого объекта | Нет result/snippet/count leak | ✅ automated | security: `tests/security/api-security.test.ts` — API security search never leaks titles, snippets or counts of out-of-scope objects (T159)<br>integration: `tests/integration/overview/scope-regressions.test.ts` — overview scope regressions per-project task counts follow tasks.read; profile reviews need characters.read (T159)<br>integration: `tests/integration/platform/search-views.test.ts` — search page (S11) never leaks results, snippets or counts of hidden records (T159) |
| T160 | User URL javascript/data/file | Validation reject и safe renderer | ✅ automated | security: `tests/security/api-security.test.ts` — API security rejects javascript:, data: and file: links on every URL-accepting API, storing nothing (T160)<br>unit: `packages/application/src/knowledge/rich-text.test.ts` — rich text normalisation (§14 structured rich text, no HTML) rejects javascript:, data: and file: links instead of keeping them (T160)<br>unit: `apps/web/src/features/knowledge/inline-markup.test.ts` — editor inline markup never turns javascript:, data: or file: URLs into links and keeps escaped characters literal (T160) |
| T161 | CSRF mutation без token/origin | Rejected, данные не изменены | ✅ automated | security: `tests/security/api-security.test.ts` — API security rejects state changes without CSRF token or from another origin, with zero mutation (T161)<br>integration: `tests/integration/identity/auth.test.ts` — CSRF and session rejects mutations without a valid CSRF token or from another origin, revoking nothing (T161) |
| T162 | Concurrent dirty form | Conflict dialog, собственный ввод не затёрт | ✅ automated | e2e: `tests/e2e/specs/conflict-drawers.spec.ts` — task drawer: live update keeps the typing, second save gets the conflict dialog (T162)<br>e2e: `tests/e2e/specs/conflict-drawers.spec.ts` — goal drawer: live update keeps the typed target, second save gets the conflict dialog (T162)<br>e2e: `tests/e2e/specs/conflict.spec.ts` — second save gets the conflict dialog and keeps the typed input (T162) |
| T163 | Same idempotency key different body | 409, второй эффект отсутствует | ✅ automated | integration: `tests/integration/finance/ledger.test.ts` — financial entries: lifecycle and immutability create is idempotent; same key with a different body is 409 and has no second effect (T163)<br>integration: `tests/integration/organization/projects.test.ts` — projects the same key with a different body is 409 and creates nothing (T163)<br>integration: `tests/integration/production/content.test.ts` — content items (S22–S24, §10.1) creates an Idea draft with the minimum fields, replays idempotently and audits; same key with another body is 409 with no second effect (T163)<br>integration: `tests/integration/work/tasks.test.ts` — task create/edit creates once per Idempotency-Key, defaults priority to Normal and requires If-Match on edits (T163, T164) |
| T164 | Missing/old If-Match | 428/412 по API contract | ✅ automated | integration: `tests/integration/organization/projects.test.ts` — projects requires If-Match and rejects stale versions (T164)<br>integration: `tests/integration/production/content.test.ts` — content items (S22–S24, §10.1) If-Match: missing → 428, stale → 412 (T164)<br>integration: `tests/integration/work/tasks.test.ts` — task create/edit creates once per Idempotency-Key, defaults priority to Normal and requires If-Match on edits (T163, T164) |
| T165 | Browser offline | Pending unsaved показан честно, critical action не queue silently | ✅ automated | e2e: `tests/e2e/specs/offline.spec.ts` — saving while offline fails honestly and is not replayed later (T165)<br>e2e: `tests/e2e/specs/offline.spec.ts` — a drawer form keeps its input and explains the failed save while offline (T165) |
| T166 | Keyboard-only Review/Publication flow | Все действия доступны, focus возвращается правильно | ✅ automated | e2e: `tests/e2e/specs/review-publication-keyboard.spec.ts` — review, approve and publish with the keyboard only (T166) |
| T167 | 200% zoom, 360 px | Нет body overflow, элементы доступны, errors видимы | ✅ automated | e2e: `tests/e2e/specs/mobile-zoom-and-narrow.spec.ts` — 360 px phone › main screens fit 360 px and validation errors stay visible (T167)<br>e2e: `tests/e2e/specs/mobile-zoom-and-narrow.spec.ts` — 200 % browser zoom of a 1440×900 window › main screens reflow at 200 % zoom and validation errors stay visible (T167) |
| T168 | Dark/Light/Reduced Motion | Контраст проверен, status текстовый, ненужная анимация отключена | ✅ automated | e2e: `tests/e2e/specs/preferences.spec.ts` — theme › dark and light themes switch from Personal Settings; badges keep text and contrast (T168)<br>e2e: `tests/e2e/specs/preferences.spec.ts` — reduced motion › with reduced motion, drawers, dialogs and the sidebar do not animate (T168) |
| T169 | Empty production workspace | Объясняющие CTA, графики без fake данных | ✅ automated | integration: `tests/integration/insights/semantic.test.ts` — semantic layer: comparison, scope and finance shows an empty workspace without fake numbers or charts (T169)<br>integration: `tests/integration/overview/overview.test.ts` — overview (S08) an empty workspace shows the setup checklist and no fake KPIs or charts (T169)<br>e2e: `tests/e2e/specs/empty-workspace.spec.ts` — empty workspace (T169) › Overview offers the first steps instead of numbers<br>e2e: `tests/e2e/specs/empty-workspace.spec.ts` — empty workspace (T169) › Projects, Content and Accounts explain themselves and open their create forms<br>+6 more |
| T170 | Staging load profile | Latency/queue thresholds измерены, report приложен | ❌ measured, not met | Measured with the load harness (tests/performance: `pnpm perf:seed` / `perf:stack` / `perf:run`) at full §28.3 volume on the 4-CPU development container, not the staging hardware. The setup was three web processes behind Caddy plus the worker, with the access cache, the analytics read model and the list indexes. In the steady state (30 reads/s + 5 writes/s) all 6 threshold classes are met (p95: list 186 ms, detail 127 ms, search 295 ms, analytics 79 ms from the warmed read model, writes 280 ms, heavy 161 ms). The ×3 burst needs about 6.4 cores and saturates the 4-CPU host, so over all measured phases 0 of 6 classes are met (p95 7–12 s). Queue lag stayed within 2.3 s in the steady state. Report: docs/acceptance/performance-report.md, with earlier runs next to it. Re-run on staging before sign-off. |
| T171 | Sensitive logs audit | Нет passwords, MFA, raw contact notes, signed URLs | ✅ automated | integration: `tests/integration/ofm/contacts.test.ts` — OFM contacts (S45/S46) T171: raw contact notes never reach notifications, audit diffs, search, outbox payloads or job payloads<br>integration: `tests/integration/platform/logs.test.ts` — sensitive logs audit (T171) never writes passwords, tokens, signed URLs or imported/exported values to logs or audit<br>unit: `packages/application/src/core/services.test.ts` — log redaction (T171) removes credentials, MFA codes, tokens, notes and URL signatures |
| T172 | Dramora недоступна/не настроена | Все функции CRM работают, нет обращения к её API | ✅ automated | security: `tests/security/external-integrations.test.ts` — no external platform integrations (T172) application code makes no outbound HTTP calls except same-origin browser requests<br>security: `tests/security/external-integrations.test.ts` — no external platform integrations (T172) no Dramora or social-platform endpoint is referenced by application code (fixtures aside)<br>security: `tests/security/external-integrations.test.ts` — no external platform integrations (T172) runtime core flows work with no integration configured and never reach the network; platform links stay links |
